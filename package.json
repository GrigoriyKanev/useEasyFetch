{
  "name": "use-easy-fetch",
  "version": "0.1.0",
  "description": "Лёгкая утилита для удобной работы с Fetch API (FormData/JSON) в TypeScript/JavaScript.",
  "type": "module",
  "main": "dist/useEasyFetch.js",
  "types": "dist/useEasyFetch.d.ts",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2",
    "rimraf": "^5.0.0"
  }
}